{
    "storage-driver": "overlay2",
    "default-address-pools" : [
      {
        "base" : "{{ docker_address_pool }}",
        "size" : 24
      }
    ]{% if not docker_manage_iptables %},
    "iptables": false
    {%- endif %}

}
